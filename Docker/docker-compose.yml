version: '2.1'
services:
  api-test:
    container_name: twitter_api_test_java
    build:
      context: .
      dockerfile: ./jdk/Dockerfile
    env_file:
      - ./env.conf
    ports:
      - "30000-45000:8765"
    volumes:
      - ./:/code
    entrypoint: ./entrypoint.sh
